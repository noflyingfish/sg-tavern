spring:
  application:
    name:
  datasource:
    url:
    username:
    password:
  jpa:
    hibernate:
      ddl-auto:
      naming.physical-strategy:
    show-sql:
    properties:
      hibernate:
        dialect:
    open-in-view:
discord.bot.token:

guildId:

userId:
  owner:
  dev:

channelId:
  intro:
  rules:
  admin-bot:
  upcoming-event:

role:
  event-organiser:
  new-joiner:
