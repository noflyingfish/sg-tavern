spring:
  application:
    name:
  datasource:
    url:
    username:
    password:
  jpa:
    hibernate:
      ddl-auto:
      naming.physical-strategy:
    show-sql:
    properties:
      hibernate:
        dialect:
    open-in-view:
discord.bot.token:

guildId:

userId:
  owner:

channelId:
  intro:
  rules:
  admin-bot:

role:
  event-organiser:
  new-joiner:
